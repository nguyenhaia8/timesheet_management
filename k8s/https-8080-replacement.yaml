apiVersion: v1
kind: Service
metadata:
  name: timesheet-https-8080
  namespace: timesheet-management
  labels:
    app: timesheet-app
spec:
  type: LoadBalancer
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: https
  selector:
    app: nginx-https-direct
